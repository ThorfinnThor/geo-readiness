"""GEO AI Search Readiness — deterministic scan & scoring engine.

This package hosts the Python engine that runs on the worker service:
safe crawl, extraction, business-profile rules, deterministic cluster
generation, coverage, scoring, actions, and report build.

No external AI-search providers and no hidden LLM calls (Golden Rules §0A).
"""

__version__ = "0.0.0"
